import { changeCase } from '../../../helpers/change-case';
import { module, test } from 'qunit';

module('Unit | Helper | change case');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = changeCase(42);
  assert.ok(result);
});
