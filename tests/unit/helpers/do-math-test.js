import { doMath } from '../../../helpers/do-math';
import { module, test } from 'qunit';

module('Unit | Helper | do math');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = doMath(42);
  assert.ok(result);
});
