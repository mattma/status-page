import { medianValue } from '../../../helpers/median-value';
import { module, test } from 'qunit';

module('Unit | Helper | median value');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = medianValue(42);
  assert.ok(result);
});
