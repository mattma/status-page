import { classnameSwitch } from '../../../helpers/classname-switch';
import { module, test } from 'qunit';

module('Unit | Helper | classname switch');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = classnameSwitch(42);
  assert.ok(result);
});
